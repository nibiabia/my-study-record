# 验证回文串
# 给你一个字符串 s，最多 可以从中删除一个字符。

# 请你判断 s 是否能成为回文字符串：如果能，返回 true ；否则，返回 false 。


# 示例 1：

# 输入：s = "aba"
# 输出：true
# 示例 2：

# 输入：s = "abca"
# 输出：true
# 解释：你可以删除字符 'c' 。
# 示例 3：

# 输入：s = "abc"
# 输出：false

思路：
所谓的回文字符串，是指具有左右对称特点的字符串，例如 "abcba" 就是一个回文字符串。

使用双指针可以很容易判断一个字符串是否是回文字符串：令一个指针从左到右遍历，一个指针从右到左遍历，这两个指针同时移动一个位置，每次都判断两个指针指向的字符是否相同，如果都相同，字符串才是具有左右对称性质的回文字符串。
本题的关键是处理删除一个字符。在使用双指针遍历字符串时，如果出现两个指针指向的字符不相等的情况，我们就试着删除一个字符，再判断删除完之后的字符串是否是回文字符串。

在判断是否为回文字符串时，我们不需要判断整个字符串，因为左指针左边和右指针右边的字符之前已经判断过具有对称性质，所以只需要判断中间的子字符串即可。

在试着删除字符时，我们既可以删除左指针指向的字符，也可以删除右指针指向的字符。

# 自己根据思路敲的：


class Solution:
    def validPalindrome(self, s: str) -> bool:
        left = 0
        right = len(s) - 1
        s_list = list(s)
        while left < right:
            if s_list[left] == s_list[right]:
                left += 1
                right -= 1
            else:
                s_list.pop(left)
                left += 1


# 哈吉米给的纠错：
1.在循环中动态修改列表：你使用了 s_list.pop(left)。这是一个很危险的操作，因为 pop 删掉一个元素后，列表中后面的所有元素索引都会整体前移一位。
此时你的 right 指针没有跟着变，就会指向错误的位置，甚至导致索引越界。
2.如何顺畅地把思路翻译成代码？

当遇到左右指针指向的字符不相等时（也就是进入了你的 else 分支），根据题目要求，我们可以删除一个字符。这时其实面临两种平行的选择，并且只要其中一种选择能构成回文串，结果就是 True：

尝试跳过左边的字符（假装删掉了 left），判断剩下的部分是不是回文串。

尝试跳过右边的字符（假装删掉了 right），判断剩下的部分是不是回文串。

在 Python 里，不需要特意把字符串转换成 list，直接利用字符串切片功能来提取剩下的部分，然后再用[::-1] 翻转对比，代码会非常简洁：

标答：


class Solution:
    def validPalindrome(self, s: str) -> bool:
        left = 0
        right = len(s) - 1

        # 只要 left < right 就继续，相遇了就说明检查完了
        while left < right:
            if s[left] == s[right]:
                # 字符相等，指针继续向中间移动
                left += 1
                right -= 1
            else:
                # 遇到不相等的字符了，尝试两种“删除”的可能性

                # 可能性 1：跳过左指针指向的字符 (截取 left+1 到 right 的部分)
                # 注意切片的右边界是开区间，所以要写 right+1
                skip_left = s[left+1: right+1]

                # 可能性 2：跳过右指针指向的字符 (截取 left 到 right-1 的部分)
                skip_right = s[left: right]

                # 只要这两种情况里有一个是完美的回文串，这道题就满足条件
                return skip_left == skip_left[::-1] or skip_right == skip_right[::-1]

        # 如果整个循环走完都没有触发不相等，说明原始字符串本身就是个回文串
        return True
